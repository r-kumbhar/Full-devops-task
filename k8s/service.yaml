apiVersion: v1
kind: Service

metadata:
  name: foodfiesta-service
  namespace: prod

spec:
  type: NodePort

  selector:
    app: foodfiesta

  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080